

<h1>Message List</h1>

<% @messages.each do |message| %>
  <div class="message">
    <h3><%= message.title %> <small>(by: <%= message.author %>)</small></h3>
    <p>
      <%= message.content %>
    </p>
  </div>
<% end %>